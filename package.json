{
  "name": "talk-azure-terraform-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Talk slides: bootstrapping a secure Azure remote backend for Terraform with Bicep",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "audit": "npm audit --audit-level=high"
  },
  "dependencies": {
    "@fontsource/atkinson-hyperlegible": "^5.3.0",
    "reveal.js": "^6.0.2"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  }
}
